import { NavEffectDirective } from './nav-effect.directive';

describe('NavEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new NavEffectDirective();
    expect(directive).toBeTruthy();
  });
});
